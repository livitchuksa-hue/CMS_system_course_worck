<UserControl x:Class="CMSBuilder.Views.Editor.EditorCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource WorkspaceBrush}"
             AllowDrop="True"
             Drop="EditorCanvas_Drop"
             DragOver="EditorCanvas_DragOver">

    <Grid Margin="16">
        <Border Background="{StaticResource WorkspaceBrush}"
                BorderBrush="{StaticResource BorderStrongBrush}"
                BorderThickness="1"
                CornerRadius="10">
            <ScrollViewer x:Name="CanvasScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <Canvas x:Name="DesignCanvas"
                        Width="{Binding CanvasWidth}"
                        Height="{Binding CanvasHeight}"
                        Background="{Binding CanvasBackground, Converter={StaticResource CssBrush}}"
                        AllowDrop="True"
                        Drop="EditorCanvas_Drop"
                        DragOver="EditorCanvas_DragOver"
                        MouseLeftButtonDown="DesignCanvas_MouseLeftButtonDown"
                        MouseMove="DesignCanvas_MouseMove"
                        MouseLeftButtonUp="DesignCanvas_MouseLeftButtonUp"
                        MouseLeave="DesignCanvas_MouseLeave">

                    <ItemsControl ItemsSource="{Binding CanvasElements}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="4" CornerRadius="6" Background="Transparent"
                                        Width="{Binding Width, Converter={StaticResource NullableDoubleToSize}}"
                                        Height="{Binding Height, Converter={StaticResource NullableDoubleToSize}}"
                                        MouseLeftButtonDown="Element_MouseLeftButtonDown"
                                        Cursor="SizeAll">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="MinWidth" Value="40" />
                                            <Setter Property="MinHeight" Value="24" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                                    <Setter Property="BorderThickness" Value="2" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <ContentControl Content="{Binding Type, Converter={StaticResource ElementTypeToView}}"
                                                    DataContext="{Binding}" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
